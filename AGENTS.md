# Light Dashboard 协作协议

目标：让多个 Agent 在没有中央调度服务的情况下，共享事实、避免互相覆盖，并把结果交给人类流式监督和最终验收。

## 只有一个事实源

任务只存在于 `docs/tasks/` 的 Markdown 文件中。不要另建数据库、状态页、任务总表或私有进度账本。网页和 CLI 都只是这批文件的入口。

## 开始工作

这些规则适用于直接收到需求的主执行者，也适用于被派发任务的 Agent；不要求人类预先建任务或复制特殊 Prompt。

实际修改前：

1. 有任务 id 就运行 `node tools/board/task.mjs show <id>`。
2. 没有 id 就运行 `list`，复用明显对应的任务；否则运行 `add --title "可验收结果"`。
3. 在自己的 Task MD 中写 `## 计划`，说明边界、预计修改文件和验证方式。
4. 检查当前 Git 状态和目标文件 diff。已有改动可能属于别人，不覆盖、不清理。

执行开始、范围变化、验证失败或受阻时，及时更新同一个 Task MD 的 `## 计划` / `## 进展`，写清已完成和剩余工作。聊天中的进度不能替代 Markdown。

只读问答不需要建任务。每个主执行者只维护自己的 Task MD；临时子 Agent 没有独立任务 id 时，只向主执行者回传结果，由主执行者写回原任务。无需新增 `doing`、`start`、心跳或文件锁字段。

### 主看板与 worktree

本机所有执行者应共享主工作区的 `docs/tasks/`，不要在每个 worktree 维护一份任务副本。代码可以在 worktree 隔离，但计划和结果仍实时写回主看板：

```powershell
node tools/board/task.mjs show <id> --root "主工作区绝对路径"
```

直接编辑 `show` 返回的 `filePath`。主工作区可由 `git rev-parse --path-format=absolute --git-common-dir` 的父目录确认。独立克隆使用自己的主工作区，并通过 Git 交换任务记录。

## 自由分流

没有固定业务线。任务可选写一个自由文本字段：

```yaml
stream: frontend
```

Agent 可以创建任意数量和层级的名字，例如 `research`、`research/memory`、`release/week-1`，也可以完全不写 `stream`。它只是视图分组和交流提示，不决定权限、优先级或目录。

不要为了分流建立新状态机。协作安全来自明确的任务和文件所有权：一个任务一个文件，同一文件同一时刻一个负责人。

## 模型分工

一种经过实践的默认分工是：

- **Fable 5 / 协调 Agent**：对接模糊需求、把握轻重、切分边界、分配文件所有权、监督和审查成果。
- **GPT-6 / 执行 Agent**：承担边界清楚、工作量大、需要大量代码和验证证据的实现任务。
- **轻量模型**：处理搜索、整理、机械检查等低风险小任务。

协调者不应主要消耗在机械执行上，执行者也不应替团队决定模糊的产品方向。这个分工是默认策略，不是身份绑定：模型可以轮换角色，交接只认 Task MD、Git 状态和当前验证证据，不认聊天记忆。

## 执行与交接

- 开工、范围变化、验证失败、受阻和关键结果都写入 `## 进展`。
- 不把聊天中的“正在做”当成团队状态；下一位 Agent 应从 Task MD 与 Git 状态恢复。
- 互不重叠的任务可以并行。文件或事实所有权重叠时，先完成、提交并交接，再由下一位继续。
- 负责人开工前确认预计修改文件；与活动任务重叠时安排先后，不允许两个任务同时改同一文件后再猜测合并。
- 共享工作区的未提交修改都可能属于别人。只改自己的局部；禁止 reset、checkout 覆盖或拿旧版本整文件替换当前文件。
- Agent 自述完成不算证据。验证必须对应任务要求，并记录命令、结果或可检查产物。

Fable、GPT-6 或其他模型都可以担任协调者或执行者。角色来自当前任务，不绑定某个永久模型。复杂任务可以由协调 Agent 拆成多个文件边界清晰的任务；简单任务直接完成，不为委派而委派。

## 大型委派合同

给子 Agent 或独立执行线程的提示词至少写清：

1. 一个明确的最终结果。
2. 允许修改的文件或目录。
3. 禁止触碰的文件、状态和外部系统。
4. 逐条可验证的验收条件，包括具体命令和期望结果。
5. 任务 id、主工作区位置和结果写回方式。

目标越机械，越适合委派。需要理解模糊需求、权衡架构方向、修改控制文档、使用密钥、产生费用、公开部署或删除数据的工作，不应直接甩给执行 Agent。

一个执行任务可以带自己的子 Agent，但共享文件的修改必须在该任务内部串行。子 Agent 返回短结果与证据，主执行者负责验收和写回，不复制整段对话进 Task MD。

## 监督节奏

- 等待，而不是高频轮询。让执行线程在完成、失败或需要人类输入时通知协调者。
- 只在出现真实新证据、阻碍或偏航时转向；不要为了“显得在监督”反复打断。
- 执行线程空闲后，协调者逐条复跑或检查验收条件；缺什么再 follow-up，证据齐全才接收。
- 审批类请求不自动放行。需要人类权限时，保留任务状态并明确写出阻碍。

## 证据纪律

- `exit 0`、格式漂亮的报告、静态计数或 Agent 自述都不自动等于验收通过。
- 当前代码、当前运行结果和一手来源优先于旧聊天、旧截图、历史证据或归档任务。
- 验证范围与风险匹配：小改动做聚焦检查；权限、删除、恢复、迁移、并发和持久状态需要更强证据。
- 出错时记录表面症状、底层机制、修复范围、验证边界和剩余风险，不用新状态或 fallback 掩盖根因。

## Git 与集成

- 默认只有一条长期集成分支（通常是 `main`）。需要隔离时使用 `task/<任务id>` worktree 或短期分支。
- 提交只包含自己的文件，提交信息带任务 id。任务 MD 在主工作区时可与任务分支代码分别提交。
- worktree 任务完成并通过必要验证后，可由负责集成的 Agent 合并；不必把人类 `approve` 当作代码合并前置条件。
- 合并与任务审核是两件事：任务仍进入 `review`，人类决定归档。合并也不等于授权公开部署、删除数据或执行其他外部动作。

## 完成与审核

状态只有三个：

```text
todo → review → done
       ↘ reject → todo
```

执行和验证完成后：

```powershell
node tools/board/task.mjs done <id> --feedback "结果与验证证据"
node tools/board/task.mjs show <id>
npm run board:check
```

确认状态为 `review`。人类验收后运行 `approve`，文件才进入 `archive/`。人类打回用 `reject --reason "原因"`。结果和打回记录只追加，不覆盖历史。

先写回 `review`，再提交自己的代码、测试和 Task MD；不要只在聊天里说完成，也不要把 `done` 留给人类代跑。未完成就保留 `todo` 并记录阻碍。

## 实现原则

- 一个事实只有一个所有者。先问“谁拥有这个事实、能否从已有结构推出”，再决定是否增加字段或协议。
- 网页、CLI 和报告都是投影；不要让投影成为第二个业务真相。
- 优先删除重复状态和旧路径，不用关键词判断、隐藏评审、自动补流程或更多 fallback 掩盖根因。
- 附属功能失败不能推翻已经完成且有证据的主结果。

## 权限边界

默认可自主执行可逆的本地修改、任务下发、聚焦验证、文档更新和本地提交，不逐项请示。推送遵循仓库已有授权。删除用户数据、处理密钥、产生外部费用、公开发布或其他不可逆操作，必须获得明确授权。任务协议不会扩大这些权限。
