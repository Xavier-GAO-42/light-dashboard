# Light Dashboard 工作方式

目标只有一个：让人类可以持续监督多个 Agent，把需求稳定地交付成可验证结果。流程服务于交付，不为流程本身工作。

## 唯一事实：Task Markdown

`docs/tasks/` 是唯一任务事实。网页和 CLI 直接读写这些 Markdown，不保存副本。

1. **先有任务**：实际修改开始前，有 id 就运行 `node tools/board/task.mjs show <id>`；没有 id 就先 `list`，复用明显对应的任务，否则 `add`。只读问答不建任务。
2. **持续写回**：修改前在该任务中写 `## 计划`；开工、范围变化、验证失败或受阻时更新 `## 进展`。聊天状态不能替代 Task MD。
3. **完成待审核**：实现和必要验证完成后运行 `done <id> --feedback "结果与验证证据"`，再 `show` 确认 `status: review`，运行 `npm run board:check`。执行者不自行 `approve`。
4. **人类归档**：人类验收后运行 `approve <id>`；打回用 `reject <id> --reason "原因"`。历史结果和打回记录只追加，不覆盖。

任务可以直接进入 L / G / P / R；`inbox` 只放归属未定或尚未成形的提醒，不是任务缓冲区。字段、目录和命令见 `docs/tasks/README.md` 与 `tools/board/README.md`。

## Fable 5 × GPT-6 × 人类

- **Fable 5：总协调者。** 对接需求、判断轻重、拆分任务、分配文件边界、处理跨任务冲突、审查证据并给出最终解释。
- **GPT-6：执行者。** 承接边界明确的实现、批量修改、测试、诊断和证据整理；先读任务和当前 Git 状态，持续写回，不凭自述宣布完成。
- **人类：流式监督与最终验收。** 通过网页查看 Task MD 的计划、进展与结果，随时修改任务要求；审核决定是否归档。

角色不绑定永久模型。任何 Agent 接手时只认 Task MD、工作树和当前验证结果，不把旧聊天当作权威状态。

临时子 Agent 没有独立任务 id 时，只向主执行者回传结果；主执行者负责把进展和证据写回原任务。不要让多个 Agent 同时修改同一文件。

## 并行与 Git

- 开工前列出预计修改的文件，并检查当前 `git status` 与目标文件 diff。
- 未提交改动可能属于别人；保留它们，不 reset、不 checkout 覆盖、不替别人清理。
- 互不重叠的模块可以并行；同一文件或同一状态所有者的修改必须串行。
- 小而明确的修改可以直接在当前工作区完成；需要隔离时使用 `task/<任务id>` worktree 或分支。
- `main` 是唯一长期集成分支。任务通过必要验证后可合并，但任务仍保持 `review`，直到人类审核归档。
- 提交只包含本任务文件，提交信息带任务 id。推送、公开部署或外部写入仍服从用户授权边界。

## 验证与交付

- 先验证直接相关路径；高风险状态、权限、删除、恢复和迁移需要更强验证。
- Agent 自述、命令 `exit 0` 或静态扫描都不是完整证据；证据要对应实际验收条件。
- 失败时记录表面症状、底层原因、已做修复、验证边界和剩余工作。
- 未完成就保留 `todo` 并记录阻碍，不把 `done` 留给人类代跑。

## 权限边界

默认可自主执行可逆的本地修改、聚焦验证、Task MD 更新和本地提交。删除用户数据、处理密钥、产生外部费用、公开发布或其他不可逆操作，必须先获得明确授权。
