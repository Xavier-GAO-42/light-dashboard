---
id: t-20260905-2a3c
title: 精简 README 中英文表达，去除开发过程自言自语
status: review
stream: docs
created: 2026-09-05
---
## Prompt
用户要求：仅提升 README.md 与 README.en.md 的清晰度、准确性、语法与流畅度；删除开发过程中的自言自语（如“为什么不放任务线 1 / 任务线 2”），不改变项目内容、结构、立场与事实。

## 计划

- 边界：只修改 `README.md`、`README.en.md` 和本 Task MD。不碰 AGENTS.md、CLAUDE.md、代码、网页、图片。
- 保留：标题层级与顺序、图片顺序与图注、链接、命令、模型名、数字、架构描述、适用边界。
- 精简：作者的话中的重复叙述；`stream` 一节中“为什么不放任务线 1 / 任务线 2”这段开发决策自述，改为中性的示例小节（保留 h3 层级与位置、保留 yaml 示例）；“为什么现在可行”中与作者的话重复的内容在不丢事实前提下收紧。
- 不新增观点、口号或推测；中英文表达同一组事实。
- 验证：`ls` 检查本地链接与图片存在；`git diff --stat` 只含两份 README 与本 Task MD；`git diff` 逐段核对命令与路径未变；`npm run board:check`。

## 进展

- 2026-09-05 已通读 AGENTS.md、两份 README。核实事实：网页每 2 秒刷新（index.html:69）、端口 4790（server.mjs:37）、四张图片与所有本地链接目标均存在。
- 发现中英文既有差异，按“保留命令与标题”要求不改动，仅在结果中记录：中文标题“30 秒开始”对应英文“Three-minute start”；中文示例命令 `--stream frontend` 对应英文 `--stream product/frontend`。
- 2026-09-05 已改写 README.md 与 README.en.md。验证结果：
  - `git diff --stat`：仅 README.md、README.en.md 各 34 行变动，另有本 Task MD 新增；无其他文件。
  - 标题层级与顺序：与 HEAD 逐行对比，唯一差异是 `### 为什么不放“任务线 1 / 任务线 2”` → `### stream 示例`（英文 `### Why there is no “Task Lane 1 / Task Lane 2”` → `### Example streams`），层级与位置不变，正文删去“空编号会暗示…”的设计自述，yaml 示例与解释保留。
  - 图片与图注、全部代码块（含命令与架构图）、全部链接目标：与 HEAD 逐行对比完全一致。
  - 12 个本地链接/图片目标全部存在。
  - `npm run board:check`：检查通过：6 个任务。

## 结果

仅改写 README.md 与 README.en.md 的表述：精简作者的话与重复叙述，将“为什么不放任务线 1 / 任务线 2”改为中性的 stream 示例小节（层级位置不变，yaml 示例保留）。验证：git diff 仅两份 README + 本 Task MD；标题（除该小节改名）、图片图注、代码块、链接目标与 HEAD 逐行一致；12 个本地链接目标存在；board:check 通过。既有中英差异未改动：中文“30 秒开始”对应英文“Three-minute start”；中文示例 --stream frontend 对应英文 --stream product/frontend。
