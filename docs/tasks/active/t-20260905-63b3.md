---
id: t-20260905-63b3
title: 补回 AGENTS 与 CLAUDE 的高价值协作提示词
status: review
owner: codex
stream: protocol
created: 2026-09-05
---
## Prompt

在不恢复固定业务线和 Kaopy 业务内容的前提下，保留原 `AGENTS.md` 与 `CLAUDE.md` 中已经验证好用的多 Agent 协作提示词。

## 计划

- 对照原始提示词，提取任务写回、主看板/worktree、文件所有权、模型分工、并行、长任务合同、监督节奏、证据验收、集成与权限边界。
- 将完整规则放入 `AGENTS.md`，将每次会话必须命中的短规则浓缩到 `CLAUDE.md`，避免两份文件机械重复。
- 扫描 Kaopy 专有名词和固定线 schema，运行 Markdown 协议检查后提交并推送。

## 进展

- 已确认当前仓库干净，现有自由 `stream` 和最小任务 schema 保持不变。
- 已从原提示词保留主看板/worktree 分离、文件所有权、模型分工、大型委派合同、等待式监督、证据纪律、自主集成与权限边界。
- `AGENTS.md` 保留 132 行完整规则，`CLAUDE.md` 收敛为 13 行高频入口；未恢复 Kaopy 专有名词、固定线、阶段或标签 schema。
- `board:check`、项目专有词扫描与 Git diff 检查通过。

## 结果

已补回经实践验证的协作提示词：AGENTS 保留完整执行纪律，CLAUDE 保留高频短合同；未恢复固定业务线或项目专有内容
