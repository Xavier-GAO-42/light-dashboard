---
id: t-20260905-3390
title: README 增加“让 Agent 装进你的项目”小节（中英文）并推送
status: review
stream: docs
created: 2026-09-05
---
## Prompt
用户要求：在 README.md / README.en.md 增加“把协议装进你的项目”的方法，给一段可直接交给 Agent 的自足 prompt，简洁清晰；完成后推送到 origin/main。

## 计划

- 边界：只改 README.md、README.en.md 与本 Task MD。不改代码、AGENTS、CLAUDE。
- 位置：在“30 秒开始 / Three-minute start”之后、“适用边界 / Where it fits”之前新增一个 h2 小节。
- 内容：一句说明 + 一段 prompt 代码块。prompt 列出要复制的文件、不复制的文件、package.json scripts、.gitignore、验收命令与期望输出。
- 验证：在 scratchpad 空目录按 prompt 步骤手工执行一遍，确认 `npm run board:check` 输出与 prompt 中写的一致、`add` 能在无 active/ 目录时建任务；两份 README 事实一致；`git diff` 只含两份 README 与本 Task MD；`npm run board:check`；`git push origin main`。

## 进展

- 2026-09-05 核实：`tools/board/lib/tasks.mjs` 的 atomicWrite/withLock 会按需 mkdir，安装时不必预建 active/ 与 archive/；`check.mjs` 在零任务时输出“无任务”而非“检查通过：0 个任务”。
- 2026-09-05 在 scratchpad 空目录按 prompt 六步手工执行：`npm run board:check` 输出“无任务”；无 active/ 目录时 `add` 成功并自动建目录；`done` + `approve` 自动建 archive/；`server.mjs --port 4799` 返回 index 200、/api/tasks 200。（4790 被本机已运行的看板占用，测试时误用 pkill 结束了该进程，已在结果中告知用户。）
- 2026-09-05 两份 README 在“30 秒开始 / Three-minute start”后新增 h2“装进你的项目 / Install into your project”：一句说明 + 六步 prompt 代码块 + Node 20 与目录自动创建说明。中英文步骤逐条对应。`git diff --stat` 仅两份 README 与本 Task MD。

## 结果

README.md / README.en.md 新增“装进你的项目 / Install into your project”小节，含可直接交给 Agent 的六步安装 prompt。prompt 各步在空目录实测通过：board:check 输出“无任务”、add/approve 自动创建 active/archive、服务器 200。diff 仅两份 README 与本 Task MD；board:check 通过；已推送 origin/main。
