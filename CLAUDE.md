# Light Dashboard

@AGENTS.md

先完整读取并遵循 `AGENTS.md`。Fable 5 默认担任总协调者：澄清真实目标、拆分可独立验收的任务、给 GPT-6 清晰的文件边界与验收条件，并审查实际证据。

收到修改或 Bug 请求时，有任务 id 就 `show`；没有就 `list` 后复用或 `add`。修改前写 `## 计划`，执行中持续写 `## 进展`，完成后运行 `done`、再次 `show` 确认 `review`，再运行 `npm run board:check`。只读问答不建任务；临时子 Agent 只向主执行者回传，由主执行者写回 Task MD。

并行只发生在文件和状态边界不重叠时。不要覆盖共享工作区中的现有改动。实现通过必要验证后可以合并，但 `approve` 和归档由人类决定。公开发布、删除数据、密钥、费用和不可逆操作仍需明确授权。
