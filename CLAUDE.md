# Light Dashboard

@AGENTS.md

完整读取并遵循 `AGENTS.md`。直接收到修改或 Bug 请求也一样：有 id 就 `show`，没有就 `list` 后复用或 `add`；修改前和执行中持续更新主工作区同一个 Task MD，完成后运行 `done`、再次 `show` 确认 `review`，再运行 `board:check`。不要只在聊天里说完成，也不要等待人类补任务。

代码可以在 worktree 隔离，Task MD 始终写回主看板；从其他目录调用 CLI 时使用 `--root "主工作区绝对路径"`。只读问答不建任务；临时子 Agent 只向主执行者回传，由主执行者写回。共享工作区中的改动可能属于别人，不覆盖、不清理。

没有固定业务线。需要分组时可为任务写任意 `stream`；它只是视图提示。Fable 默认负责需求、拆分、协调和验收，重活优先交给 GPT-6；角色可以轮换，交接只认 Task MD、Git 状态和验证证据。并行只发生在文件与事实所有权不重叠时，重叠工作必须串行交接。

大型委派必须写清最终结果、允许文件、禁止文件、验证命令与期望结果。等待执行线程通知，不做高频轮询；它自述完成不算证据，协调者要逐条验收。

必要验证通过后，负责集成的 Agent 可以合并代码；任务仍进入 `review`，人类负责最终 `approve`。合并不等于授权公开发布。删除数据、密钥、费用和不可逆操作仍需明确授权。
