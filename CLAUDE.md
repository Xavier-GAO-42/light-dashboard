# Light Dashboard

@AGENTS.md

完整读取并遵循 `AGENTS.md`。不要把聊天当作共享状态：先读取对应 Task MD 和 Git 状态，修改前写计划，执行中写进展，完成后提交 `review` 并给出验证证据。

没有固定业务线。需要分组时可为任务写任意 `stream`；它只是视图提示。并行只发生在文件和事实所有权不重叠时，重叠工作必须串行交接。Fable 可以协调，GPT-6 可以执行，也可以反过来；角色由任务边界决定。

人类负责最终 `approve`。公开发布、删除数据、密钥、费用和不可逆操作仍需明确授权。
